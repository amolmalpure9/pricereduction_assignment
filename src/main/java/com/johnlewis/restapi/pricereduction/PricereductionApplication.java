package com.johnlewis.restapi.pricereduction;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * 
 * @author Amol This application is used for returning the price reduced items
 *         in sorted order
 */

@SpringBootApplication
public class PricereductionApplication {

	public static void main(String[] args) {
		SpringApplication.run(PricereductionApplication.class, args);
	}

}
